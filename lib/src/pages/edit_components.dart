import 'package:flutter/material.dart';

class EditComponents extends StatelessWidget {
  const EditComponents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Edit view"),
    );
  }
}
