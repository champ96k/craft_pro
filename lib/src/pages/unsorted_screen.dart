import 'package:flutter/material.dart';

class UnsortedScreen extends StatelessWidget {
  const UnsortedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('UnsortedScreen');
  }
}
