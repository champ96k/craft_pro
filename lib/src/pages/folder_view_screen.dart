import 'package:flutter/material.dart';

class FolderViewScreen extends StatelessWidget {
  const FolderViewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("FolderViewScreen");
  }
}
