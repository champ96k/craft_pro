import 'package:flutter/material.dart';

class TempletsScreen extends StatelessWidget {
  const TempletsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("TempletsScreen");
  }
}
