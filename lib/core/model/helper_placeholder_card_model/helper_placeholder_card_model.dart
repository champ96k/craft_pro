import 'package:flutter/material.dart';

class HelperPlaceholderCardModel {
  final String title;
  final String subTitle;
  final Color color;

  HelperPlaceholderCardModel({
    required this.title,
    required this.subTitle,
    required this.color,
  });
}
